<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wdowiak.financemanager">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".ui.login.LoginActivity"
            android:label="Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity android:name=".accounts.AccountDetailActivity" />
        <activity android:name=".categories.CategoryAddEditActivity" />
        <activity android:name=".categories.CategoryDetailActivity" />
        <activity android:name=".currencies.CurrencyAddEditActivity" />
        <activity android:name=".currencies.CurrencyDetailActivity" />
        <activity android:name=".groups.GroupAddEditActivity" />
        <activity android:name=".groups.GroupDetailActivity" />
        <activity android:name=".transactions.TransactionAddEditActivity" />
        <activity android:name=".transactions.TransactionDetailActivity" />
        <activity android:name=".transaction_statuses.TransactionStatusAddEditActivity" />
        <activity android:name=".transaction_statuses.TransactionStatusesDetailActivity" />
    </application>

</manifest>